services:
  - type: web
    name: ai-video-editor
    runtime: python
    buildCommand: "pip install flask gunicorn groq"
    startCommand: "gunicorn app:app --bind 0.0.0.0:$PORT"
    envVars:
      - key: GROQ_API_KEY
        sync: false
